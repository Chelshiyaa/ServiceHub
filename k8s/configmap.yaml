apiVersion: v1
kind: ConfigMap
metadata:
  name: servicehub-config
  namespace: servicehub
data:
  NODE_ENV: "production"
  PORT: "5000"
  MONGODB_URI: "mongodb://mongodb:27017/servicehub"
  # MongoDB service name matches the MongoDB service below

